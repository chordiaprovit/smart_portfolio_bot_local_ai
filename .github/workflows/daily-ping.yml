name: Keep Alive

on:
  schedule:
    - cron: "*/15 * * * *"    # every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl app
        run: curl -s https://trendiq.streamlit.app/ > /dev/null
